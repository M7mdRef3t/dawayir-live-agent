# Optional: override backend WebSocket URL for production or remote backend.
# Local development works without this file (defaults to ws://localhost:8080).
VITE_BACKEND_WS_URL=wss://your-cloud-run-service-url
